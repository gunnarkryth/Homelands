import { Box, Typography } from "@mui/material";

export const Logo = () => {
  return (
    <Box>
      <Typography variant="h1" color="initial">
        Homelands
      </Typography>
    </Box>
  );
};
