import { Box, TextField, IconButton } from "@mui/material";

export const SearchBar = () => {
  return (
    <Box>
      <TextField
      variant="out"
      ></TextField>;
      <IconButton />
    </Box>
  );
};
